name: Test and Deploy to VPC

on:
  push:
    branches:
      - main

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📦 Install Dependencies
        run: npm ci

      - name: 🧪 Run Unit Tests
        run: npm test -- --ci

      - name: 📤 Copy project to VPC via SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPC_HOST }}
          username: ${{ secrets.VPC_USER }}
          key: ${{ secrets.VPC_SSH_KEY }}
          source: "."
          target: "/home/${{ secrets.VPC_USER }}/smart-editor-app"

      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPC_HOST }}
          username: ${{ secrets.VPC_USER }}
          key: ${{ secrets.VPC_SSH_KEY }}
          script: |
            cd ~/smart-editor-app
            docker-compose down
            docker-compose up --build -d
